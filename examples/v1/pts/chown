#!/usr/local/bin/perl

use AFS;

print "name: "; chop($name=<STDIN>);
print "new owner: ";chop($new=<STDIN>);

  $pts = AFS::PTS::new;

  print "AFS::PTS::new AFS::CODE = $AFS::CODE\n";

   $success = $pts ->chown($name,$new);

  print "chown AFS::CODE = $AFS::CODE\n";

  print "success  = $success\n";

 $pts = '';


